[project]
name = "moss-soundeffect-v2"
description = "Text-to-sound-effect nodes for MOSS-SoundEffect v2. Picks a local model and runs inference in a compatible Python, creating one beside the node when needed."
version = "0.2.0"
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Repository = "https://github.com/HuizhiXu/comfyui-moss-soundeffect-v2"

[tool.comfy]
# 在 https://registry.comfy.org 注册。个人页上是 @你的id，这里不要带 @。
# name 和 PublisherId 第一次发布后都不能改。name 不要包含 ComfyUI。
PublisherId = "sophiaxu"
DisplayName = "MOSS SoundEffect v2"
